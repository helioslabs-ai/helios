export async function setupCommand() {
  // TODO: implement genesis wizard using @clack/prompts
  // Steps: OKX creds → wallet IDs → AI key → Supabase → deploy contract → register agents → write .env
  console.log("Setup wizard not yet implemented — see .claude/plans/scaffold-quickstart.md");
}
